import React from 'react';

function Title(props) {
    return(
        <div>
        <h1>Giphy</h1>
        <h4>Click on the button if you want to see some gifs!</h4>
        </div>
    )
}

export default Title;